#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
main - Entry point
return: 0
*/

int main(void)
{
    char ch;

    for (ch = 'a'; ch <= 'z'; c++)
        putchar(ch);
    putchar('\n');
    return (0);
}
